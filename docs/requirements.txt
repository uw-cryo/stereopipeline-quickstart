# Docs-only dependencies for ReadTheDocs.
# We don't install asp_plot or GDAL on RTD — notebooks are pre-executed locally
# and committed with outputs, so RTD only needs the rendering stack.
# Pinned to the locally tested versions: an unpinned RTD build once resolved a
# newer sphinx-book-theme (1.4 / pydata 0.20) whose header navbar duplicated
# the sidebar search field.

sphinx==9.1.0
myst-nb==1.4.0
sphinx-book-theme==1.2.0   # hard-pins pydata-sphinx-theme==0.16.1
sphinx-design==0.7.0
sphinx-copybutton==0.5.2
sphinx-tippy==0.4.3
linkify-it-py==2.1.0
